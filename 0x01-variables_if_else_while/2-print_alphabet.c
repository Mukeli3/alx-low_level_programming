#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabet;
	for (alphabet = 'a'; x <= 'b'; x++)
	{
		putchar(alphabet);
	}
	putchar('\n');
	return (0);
}
