#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: first parameter
 * @b: second parameter
 *
 * Return: 0 on success
 */
int mul(int a, int b)
{
	int c;
	int d;
	int mul;

	mul = c * d;
	printf("%d\n", mul);
	return (0);
}
