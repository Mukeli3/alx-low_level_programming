#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: string variable whose length is to be found
 *
 * Return: the length of passed string
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
