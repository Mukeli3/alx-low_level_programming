#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: an integer
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	_putchar(abs(n));
	_putchar('\n');
	return (0);
}
