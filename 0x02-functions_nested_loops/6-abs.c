#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: a number
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	int num;

	printf("\n %d", abs(num));
	return (0);
}
