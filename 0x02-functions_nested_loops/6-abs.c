#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @int: a data type
 *
 * Return: Always 0 (Success)
 */
int _abs(int)
{
	int num;

	printf("\n %d", abs(num));
	return (0);
}
