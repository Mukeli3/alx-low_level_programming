#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @int: an integer
 *
 * Return: Always 0 (Success)
 */
int add(int, int)
{
	int num1;
	int num2;
	int sum;

	sum = num1 + num2;
	printf("%d\n", sum);
	return (0);
}
