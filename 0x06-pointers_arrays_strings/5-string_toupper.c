#include "main.h"
#include <string.h>

/**
 * *string_toupper - changes all lowercase letters
 * of a string to uppercase
 *
 * Return: 0 os success
 */
char *string_toupper(char *c)
{
	gets(c);
	strupr(c);

	return (0);
}
