#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first pointer to a char string
 * @s2: second pointer to a char string
 *
 * Return: 0 if the strings are same, otherwise 1
 */
int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);
	return (0);
}
